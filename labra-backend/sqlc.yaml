version: "2"
sql:
  - engine: "sqlite"
    queries: "sql/queries"
    schema: "sql/migrations"
    gen:
      go:
        out: "internal/api/db"
        json_tags_id_uppercase: true
        emit_json_tags: true

